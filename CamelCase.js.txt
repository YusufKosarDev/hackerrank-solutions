function camelcase(s) {
    
     if (!s) return 0;
    
    let wordCount = 1;

    for (let i = 0; i < s.length; i++) {
        
        if (s[i] === s[i].toUpperCase() && s[i] !== s[i].toLowerCase()) {
            wordCount++;
        }
    }

    return wordCount;

}