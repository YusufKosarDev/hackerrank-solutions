function jumpingOnClouds(c, k) {
    let energy = 100;
    let index = 0;
    const n = c.length;

    do {
        index = (index + k) % n;
        energy--; 
        if (c[index] === 1) {
            energy -= 2; 
        }
    } while (index !== 0);

    return energy;

}