function beautifulTriplets(d, arr) {
     let set = new Set(arr);
    let count = 0;

    for (let num of arr) {
        if (set.has(num + d) && set.has(num + 2 * d)) {
            count++;
        }
    }

    return count;

}