function insertionSort1(n, arr) {
    
    let value = arr[n - 1];   
    let i = n - 2;

    while (i >= 0 && arr[i] > value) {
        arr[i + 1] = arr[i];  
        console.log(arr.join(" "));
        i--;
    }

    arr[i + 1] = value;  
    console.log(arr.join(" "));

}