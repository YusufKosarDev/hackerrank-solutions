function beautifulBinaryString(b) {
    
    let count = 0;

    for (let i = 0; i < b.length - 2; i++) {
        if (b.substring(i, i + 3) === "010") {
            count++;
            i += 2; 
        }
    }

    return count;

}