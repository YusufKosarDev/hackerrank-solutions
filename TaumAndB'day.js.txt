function taumBday(b, w, bc, wc, z) {
    
   const whiteCost = Math.min(bc, wc + z);
    
    const blackCost = Math.min(wc, bc + z);

    
    return BigInt(b) * BigInt(whiteCost) + BigInt(w) * BigInt(blackCost);
}