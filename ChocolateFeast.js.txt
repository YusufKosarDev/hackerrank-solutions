function chocolateFeast(n, c, m) {
    
    let chocolates = Math.floor(n / c);
    
    
    let wrappers = chocolates;
    
    
    let total = chocolates;
    
    
    while (wrappers >= m) {
        
        let newChocolates = Math.floor(wrappers / m);
        
        total += newChocolates;
        
        
        wrappers = (wrappers % m) + newChocolates;
    }
    
    return total;

}