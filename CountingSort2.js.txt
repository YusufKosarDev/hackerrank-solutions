let freq = new Array(100).fill(0);
    
    
    for (let num of arr) {
        freq[num]++;
    }
    
    let result = [];
    
    
    for (let i = 0; i < freq.length; i++) {
        for (let j = 0; j < freq[i]; j++) {
            result.push(i);
        }
    }
    
    return result;